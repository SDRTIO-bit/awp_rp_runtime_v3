# 修复计划

## 0. 数据泄漏 — 立即处理

**问题**：`.awp/authoring`（含 conversations/journal/file-history/tool-audit）、`.omo/run-continuation` 已提交到公开仓库，泄露真实创作对话、会话 ID、本地路径、作者计划。

**步骤**：
- [x] 0.1 在 `.gitignore` 追加忽略规则
- [x] 0.2 `git rm --cached -r` 移除已跟踪的泄露目录和文件
- [x] 0.3 新建提交
- [x] 0.4 git filter-repo（已跳过）

---

## 1. P0 — POST/PUT 不匹配 （LLM 配置保存 405）

**文件**：`web/src/api/client.ts`

**问题**：前端 `updateLlmConfig()` 发 POST，后端只注册了 PUT `/llm-config`。

**修复**：
- [x] 1.1 在 `client.ts` 新增 `put()` 函数（对标已有 `post()`）
- [x] 1.2 `updateLlmConfig()` 改用 `put()`

---

## 2. P1 — 供应商 ID 前后端不一致

**文件**：前端 `web/src/pages/PromptStudio.tsx`，后端 `runtime/novel_llm_factory.py`

**问题**：前端用 `awp-deepseek`/`awp-opencode`/`awp-mimo`/`awp-siliconflow`，后端判断的是 `opencode`/`mimo`/`siliconflow`。前端传入的 `awp-opencode` 会掉进 DeepSeek 默认分支。

**修复**：
- [x] 2.1 前端只存 provider type（`opencode`/`mimo`/`siliconflow`/`deepseek`/`custom`），去掉 `awp-` 前缀
- [x] 2.2 `PromptStudio.tsx` 中 `LOCAL_PROVIDERS` 的 id 和后端 `_provider_choice()` 对齐

---

## 3. P1 — thinking_level 字段后端不认识

**文件**：`runtime/novel_llm_factory.py`

**问题**：API/前端用 `thinking_level`，工厂合并覆盖时用 `thinking`。

**修复**：
- [x] 3.1 `get_pi_role_config()` 中同时认 `thinking_level` 和 `thinking`，统一归一化

---

## 4. P1 — LLM 覆盖配置不会被新 Pi Host 拿到

**文件**：`runtime/novel_editor_session_manager.py`、`agent_harness/src/novel_role_host.mjs`

**问题**：`NovelLLMFactory()` 新建实例拿不到 `set_project_overrides()` 写入的数据。

**修复**：
- [x] 4.1 Python 侧：全部改为 `NovelLLMFactory.get_instance()`，不再 new
- [x] 4.2 JS 侧：`novel_role_host.mjs` 中 maxTokens 不再写死 4000，穿透后端 `connection.max_tokens`

---

## 5. P1 — 部分 API 访问了全局数据库而非项目数据库

**文件**：`runtime/novel_api.py`

**问题**：`plan_chapter`、`write_chapter`、`revise_chapter`、`batch_write`、`promote_character_state`、`autonomy_summary`、`delete_project` 调用 `self._registry()` 无参数版，路由到全局 `awp_novel_runtime.db` 而非项目绑定的 SQLite。

**修复**：
- [x] 5.1 逐一检查每个 handler，确保 `project_id` 传入 `_registry(project_id)`

---

## 6. P1 — CI smoke 命令路径无效

**文件**：`.github/workflows/ci.yml`

**问题**：`python -m awp_rp_runtime_v3.testing.run_workflow_scenarios --suite smoke --verbose` 路径不存在。

**修复**：
- [x] 6.1 删除 `run_workflow_scenarios` 命令，拆分为 python / node / smoke 三 job

---

## 7. P2 — README 与启动问题

**文件**：`README.md`、`runtime/novel_api.py` 或启动脚本相关

**问题**：README 推荐 `web.bat` 但缺前端构建产物会报错；`--db-path` 写 `AWP_DB_PATH` 但数据库工厂读 `AWP_RUNTIME_DB_PATH`。

**修复**：
- [x] 7.1 README 增加 `npm run build` 前置步骤
- [x] 7.2 统一 `--db-path` 环境变量名
